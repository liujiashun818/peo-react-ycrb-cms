import React from 'react';
import {Row, Col, Input, Radio} from 'antd';
import EBase from './e-base';

const RadioGroup = Radio.Group;

class ETextarea extends EBase {
	constructor(props) {
		super(props);
	}
}

export default ETextarea;