import React from 'react';
import {Row, Col, Input, Radio} from 'antd';
import EBase from './e-base';

const RadioGroup = Radio.Group;

class EInput extends EBase {
	constructor(props) {
		super(props);
	}
}

export default EInput;